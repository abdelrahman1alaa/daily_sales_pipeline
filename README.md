# daily_sales_pipeline
Daily Sales Pipeline is an end-to-end data pipeline project built using Apache Airflow, PostgreSQL, Python, and SQL. The pipeline extracts sales data from a PostgreSQL database, transforms it to calculate daily revenue, and loads the results into a dedicated table.
